import React from 'react';
import styled from 'styled-components';

const Styled = styled.div`
    font-weight: bold;
`;

const Logo = () => {
    return (
        <Styled>CryptEx</Styled>
    )
}

export default Logo;